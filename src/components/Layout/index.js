import React from "react";
import { IonApp, IonContent } from "@ionic/react";

import { Header } from "./Header";

export const Layout = () => (
    <IonApp>
        <Header />
        <IonContent>
            
        </IonContent>
    </IonApp>
)